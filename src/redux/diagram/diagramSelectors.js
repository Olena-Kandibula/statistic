let currentDate = new Date();
  let currentYear = currentDate.getFullYear()
  let currentMonth=currentDate.getMonth()
  console.log(currentDate.getFullYear()); //2022
  console.log(currentDate.getMonth()); // 02

  const date = {
    year: currentYear,
    month: currentMonth,
}